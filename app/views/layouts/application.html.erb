<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Vhem</title>
  </head>
  <body>
    <header>
      <div class="nav-wrapper">
        <nav>
          <a href="#" class="brand-logo right">Vhem</a>
        <ul>
          <li><%= link_to 'Home', homes_index_path%></li>
    <li><%= link_to 'All Apartments', rentals_path%></li>
    <% if realtor_signed_in? %>
    <li><%= link_to 'Profile', realtor_path(current_realtor) %></li>
    <li><%= link_to 'Create Rental', rentals_new_path %></li>
    <li><%= link_to 'Logout', destroy_realtor_session_path, :method => :delete%></li>
    <% elsif user_signed_in? %>
    <li><%= link_to 'Profile', user_path(current_user) %></li>
    <li><%= link_to 'Logout', destroy_user_session_path, :method => :delete%></li>
    <% else %>
    <li><%= link_to 'Realtors Sign In', realtor_session_path %></li>
    <li><%= link_to 'Renters Sign In', user_session_path%></li>
    <% end %>
  </ul>
</nav>
  </div>
  </header>
  <main>
  </main>
      <footer>
        <p class="notice"><%= notice %></p>
        <p class="notice"><%= alert %></p>
        <!-- Compiled and minified CSS -->
   <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0-beta/css/materialize.min.css">
   <!-- Compiled and minified JavaScript -->
   <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0-beta/js/materialize.min.js"></script>
      </footer>
    <%= yield %>
  </body>
</html>
